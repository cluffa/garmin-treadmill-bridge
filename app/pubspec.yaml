name: garmin_ftms_app
description: "Garmin FTMS Sync — control app for the ESP32 treadmill bridge."
publish_to: 'none'
version: 26.6.30+2

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^1.35.3
  web_socket_channel: ^3.0.2
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
